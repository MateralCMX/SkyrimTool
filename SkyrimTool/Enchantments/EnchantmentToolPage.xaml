<Page
    x:Class="SkyrimTool.Enchantments.EnchantmentToolPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SkyrimTool.Enchantments"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:SkyrimTool.Models"
    mc:Ignorable="d">

    <Grid Grid.Column="1" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Text="{x:Bind ViewModel.ItemCode, Mode=TwoWay}"
                Description="{x:Bind ViewModel.ItemDescription, Mode=OneWay}"
                Header="物品" />
            <TextBox
                Grid.Column="1"
                VerticalAlignment="Top"
                Text="{x:Bind ViewModel.EffectCode1, Mode=TwoWay}"
                Description="{x:Bind ViewModel.EffectDescription1, Mode=OneWay}"
                Header="效果1" />
            <TextBox
                Grid.Column="2"
                VerticalAlignment="Top"
                Text="{x:Bind ViewModel.EffectCode2, Mode=TwoWay}"
                Description="{x:Bind ViewModel.EffectDescription2, Mode=OneWay}"
                Header="效果2" />
        </Grid>
        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8">
            <Button Content="复制" Click="CopyButton_Click" />
            <TextBlock
                VerticalAlignment="Center"
                Text="{x:Bind ViewModel.ConsoleCode, Mode=OneWay}"
                IsTextSelectionEnabled="True" />
            <TextBlock VerticalAlignment="Center" Text="附魔等级：" />
            <TextBlock
                VerticalAlignment="Center"
                Text="player.setav enchanting 1000"
                IsTextSelectionEnabled="True" />
        </StackPanel>
        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <ComboBox
                        Grid.Column="0"
                        SelectedValuePath="Value"
                        DisplayMemberPath="Name"
                        ItemsSource="{x:Bind ViewModel.ItemTypes}"
                        SelectedValue="{x:Bind ViewModel.SelectedItemTypeValue, Mode=TwoWay}" />
                    <TextBox
                        Grid.Column="1"
                        Text="{x:Bind ViewModel.ItemKeyWord, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="关键字" />
                </Grid>
                <ListView
                    Grid.Row="1"
                    ItemsSource="{x:Bind ViewModel.Items}"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ItemData">
                            <Grid Padding="12" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <Button
                                        Content="物品"
                                        Click="ItemButton_Click"
                                        Tag="{x:Bind}" />
                                </StackPanel>
                                <TextBlock
                                    Grid.Column="1"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Text="{x:Bind NameZH}"
                                    IsTextSelectionEnabled="True" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBox
                    Grid.Row="0"
                    Text="{x:Bind ViewModel.EnchantmentKeyWord, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    PlaceholderText="关键字" />
                <ListView
                    Grid.Row="1"
                    ItemsSource="{x:Bind ViewModel.Enchantments}"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:EnchantmentData">
                            <Grid Padding="12" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <Button
                                        Content="效果1"
                                        Click="Effect1Button_Click"
                                        Tag="{x:Bind}" />
                                    <Button
                                        Content="效果2"
                                        Click="Effect2Button_Click"
                                        Tag="{x:Bind}" />
                                </StackPanel>
                                <TextBlock
                                    Grid.Column="1"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Text="{x:Bind EnchantmentZH}"
                                    IsTextSelectionEnabled="True" />
                                <TextBlock
                                    Grid.Column="2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Text="{x:Bind VariableZH}"
                                    IsTextSelectionEnabled="True" />
                                <TextBlock
                                    Grid.Column="3"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Text="{x:Bind EffectZH}"
                                    IsTextSelectionEnabled="True" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</Page>
