<Page
    x:Class="SkyrimTool.Potion.CreatePotionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SkyrimTool.Potion"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:SkyrimTool.Models"
    mc:Ignorable="d">

    <Grid Grid.Column="1" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                VerticalAlignment="Top"
                Text="{x:Bind ViewModel.EffectCode1, Mode=TwoWay}"
                Description="{x:Bind ViewModel.EffectDescription1, Mode=OneWay}"
                Header="效果1" />
            <TextBox
                Grid.Column="1"
                VerticalAlignment="Top"
                Text="{x:Bind ViewModel.EffectCode2, Mode=TwoWay}"
                Description="{x:Bind ViewModel.EffectDescription2, Mode=OneWay}"
                Header="效果2" />
            <TextBox
                Grid.Column="2"
                VerticalAlignment="Top"
                Text="{x:Bind ViewModel.EffectCode3, Mode=TwoWay}"
                Description="{x:Bind ViewModel.EffectDescription3, Mode=OneWay}"
                Header="效果3" />
        </Grid>
        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8">
            <Button Content="复制" Click="CopyButton_Click" />
            <TextBlock
                VerticalAlignment="Center"
                Text="{x:Bind ViewModel.ConsoleCode, Mode=OneWay}"
                IsTextSelectionEnabled="True" />
            <TextBlock VerticalAlignment="Center" Text="炼金等级：" />
            <TextBlock
                VerticalAlignment="Center"
                Text="player.setav alchemy 1000"
                IsTextSelectionEnabled="True" />
        </StackPanel>
        <TextBox
            Grid.Row="2"
            Text="{x:Bind ViewModel.KeyWord, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            PlaceholderText="关键字" />
        <ListView
            Grid.Row="3"
            ItemsSource="{x:Bind ViewModel.Enchantments}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:EnchantmentData">
                    <Grid Padding="12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <Button
                                Content="效果1"
                                Click="Effect1Button_Click"
                                Tag="{x:Bind}" />
                            <Button
                                Content="效果2"
                                Click="Effect2Button_Click"
                                Tag="{x:Bind}" />
                            <Button
                                Content="效果3"
                                Click="Effect3Button_Click"
                                Tag="{x:Bind}" />
                        </StackPanel>
                        <TextBlock
                            Grid.Column="1"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Text="{x:Bind EnchantmentZH}"
                            IsTextSelectionEnabled="True" />
                        <TextBlock
                            Grid.Column="2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Text="{x:Bind VariableZH}"
                            IsTextSelectionEnabled="True" />
                        <TextBlock
                            Grid.Column="3"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Text="{x:Bind EffectZH}"
                            IsTextSelectionEnabled="True" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
